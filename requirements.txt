# ── Core Flask ──────────────────────────────────────────────
Flask==3.1.3
Flask-Login==0.6.3
Werkzeug==3.1.8
Jinja2==3.1.6
itsdangerous==2.2.0
click==8.3.3
blinker==1.9.0
MarkupSafe==3.0.3

# ── Auth & Env ───────────────────────────────────────────────
python-dotenv==1.2.2

# ── Excel / PDF ──────────────────────────────────────────────
openpyxl==3.1.5
et_xmlfile==2.0.0
PyPDF2==3.0.1

# ── Typing ──────────────────────────────────────────────────
typing_extensions==4.15.0

# ELIMINADOS (liberan ~128MB):
# pandas, numpy, tzdata, python-dateutil, six  → reemplazados por openpyxl nativo
# requests, urllib3, certifi, charset-normalizer, idna → app usa urllib stdlib
# pydantic, pydantic_core, annotated-types     → no se usan en ningún archivo
# Flask-WTF, WTForms                           → importado pero no implementado
# anyio, sniffio, httptools                    → no se usan
# cachetools, attrs, packaging                 → no se usan
# jsonschema, jsonschema-specifications, referencing, rpds-py → no se usan
# docstring_parser, distro, jiter, typing-inspection → no se usan
# python-multipart                             → Flask maneja multipart sin esto
